# Analytic Workbench Skill

Reusable skill for human-in-the-loop analytic workflows:
- Tier 1: `marimo`
- Tier 2: `marimo + Hydra + Hamilton`
- Tier 3A: add `DVC` for caching and reproducibility
- Tier 3B: add `Kedro` for ML-heavy pipeline structure
- Tier 4: add `Dagster` or `Prefect` only when orchestration is necessary

The skill entrypoint is `SKILL.md`.
